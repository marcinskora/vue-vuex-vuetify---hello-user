export default {
    userFirstname(state) {
        return state.user.firstname;
    },
    userLastname(state) {
        return state.user.lastname;
    },
    userEmail(state) {
        return state.user.email;
    }
}